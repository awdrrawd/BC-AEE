# 服装悬停外框 & 拾取功能 实现说明

> 技術來源與致謝：本文所述的服裝懸停輪廓、alpha 遮罩命中、重疊部件輪換與 Canvas 合成描邊技術，源自並參考 **XinLian132243 / BCMod** 的服裝優化實作。  
> 上游倉庫：<https://github.com/XinLian132243/BCMod/tree/master>

对应代码位置：脚本末尾 `AppearancePicker` 类及其相关 Hook（`换装界面的整件拾取` 部分）。
调色界面里 `LayerTransformGizmo` 的图层拾取用的是同一套思路，本文以 `AppearancePicker`（换装界面整件拾取）为主线讲解，两者的核心技术是共通的。

---

## 一、要解决的问题

BC（Bondage Club）本体的换装界面里，角色预览图只是一张画出来的图，**没有任何"点这件衣服"的交互**。玩家要修改某件装备，必须先在右侧列表里翻页找到对应部件。

这个功能做的事情是：

1. 鼠标悬浮在角色身上的某件衣服上时，**沿着这件衣服贴图的实际轮廓**画一圈高亮描边（不是矩形框），并标注名称。
2. 点击身上的衣服，**直接跳转**到该部位的服装编辑界面。
3. 多件衣服贴图重叠时（比如外套盖住衬衫），支持在同一位置**连续点击轮流选中**下层的衣服。

难点在于：BC 的渲染管线只是把一堆贴图按顺序画到 canvas 上，没有保留"这一块像素属于哪件装备"这种反向索引，模组必须自己反推。

---

## 二、总体架构

```
渲染阶段（Hook）          →  数据采集              →  交互阶段（每帧 / 事件）
─────────────────         ─────────────────        ─────────────────
DrawCharacter        →   记录全身图的绘制参数
GLDrawImage /              (drawAt: x, y, zoom)
DrawImageCanvas       →   记录每张贴图的 URL 与       →  CommonMouseMove
                          绘制原点 (capture)             → 命中检测 → 悬浮高亮
AppearanceRun (后)    →   commit() 提交本帧数据      →  AppearanceClick
                                                          → 命中检测 → 跳转编辑
                                                     →  AppearanceRun (后)
                                                          → 描边绘制
```

即：**先在渲染钩子里"偷听"渲染参数，再在交互阶段用这些参数做坐标反算和像素级命中判定**。

---

## 三、数据采集：知道"画了什么、画在哪"

### 3.1 记录角色的绘制位置（`captureDraw`）

```js
mod.hookFunction("DrawCharacter", 0, (args, next) => {
    const [C, x, y, zoom, heightResize] = args;
    if (appearancePicker.isEnabled() && C === CharacterAppearanceSelection) {
        appearancePicker.captureDraw(x, y, zoom, heightResize);
    }
    return next(args);
});
```

换装界面同一帧会画两次角色（一次 zoom=4 的放大预览、一次 zoom≈1 的全身图），代码里用 `zoom > 2` 过滤掉放大预览，只保留全身图那次的参数。这组参数（角色左上角坐标 + 缩放）是后续所有"主画布坐标 ↔ 角色画布坐标"换算的基础。

### 3.2 记录每张贴图属于哪件装备（`capture` / `matchAppearanceAsset`)

BC 实际画每一层贴图时，调用的是 `GLDrawImage` 或 `DrawImageCanvas`，参数里只有贴图 URL、绘制坐标、透明度等，**没有"这是哪个 Item"的信息**。

解决办法：反查 `C.AppearanceLayers`（角色当前按层叠顺序排好的图层列表，每项都带 `.Asset` 引用），比较 URL 文件名是否以某个 `asset.Name` 开头：

```js
function matchAppearanceAsset(url) {
    const layers = CharacterAppearanceSelection?.AppearanceLayers;
    const file = url.slice(url.lastIndexOf("/") + 1).replace(/\.png$/i, "");
    let best = null;
    for (const layer of layers) {
        const asset = layer?.Asset;
        if (asset?.Name && file.startsWith(asset.Name)) {
            if (!best || asset.Name.length > best.Name.length) best = asset; // 取最长匹配，避免 Cloth1 误配到 Cloth10
        }
    }
    return best;
}
```

匹配成功后，把 `{url, 绘制原点x/y, 透明度选项opts}` 存进一个 `Map<Asset, 图层信息数组>`：

```js
capture(asset, url, x, y, opts) {
    if (opts?.Alpha === 0) return;   // 透明度为0的层不参与拾取
    let list = this.frame.get(asset) ?? [];
    list.push({ url, x: x - (opts?.TranslationX || 0), y: y - (opts?.TranslationY || 0), opts });
    this.frame.set(asset, list);
}
```

这里减去 `TranslationX/Y` 是为了把"绘制原点"还原成未做图层位移前的基准点，方便统一换算。

### 3.3 帧提交与存档（`commit`）

一帧渲染完，`AppearanceRun` 钩子的后半段调用 `commit()`，把本帧收集到的 `frame` 提升为正式的 `captures`，同时按部件组名额外存一份 `archive`：

```js
commit() {
    if (this.frameDrawAt) { this.drawAt = this.frameDrawAt; this.frameDrawAt = null; }
    if (this.frame.size > 0) {
        this.captures = this.frame;
        this.frame = new Map();
        for (const [asset, list] of this.captures) {
            this.archive.set(asset.Group.Name, { asset, list });
        }
    }
}
```

存档 `archive` 的作用：右侧部件列表悬浮时会触发"闪烁提示"效果（`CharacterAppearanceVisible` 钩子交替隐藏/显示该部件），**隐藏的那一帧根本不会画这件衣服**，`captures` 里自然也就没有它。这时描边逻辑改读 `archive` 里存的上一次可见帧数据，保证闪烁期间描边不跟着一起消失。

---

## 四、命中判定：判断鼠标/点击落在哪件衣服上

### 4.1 坐标系换算

游戏的鼠标坐标（`MouseX/Y` 或 DOM 事件坐标）是"主画布"逻辑坐标系（2000×1000），而贴图的绘制坐标是"角色画布"坐标系（500×1000 左右）。两者靠 `DrawCharacter` 记录的 `(x, y, zoom)` 做线性映射（`computeCanvasToScreen`）：

```
ox = X + xOffset*zoom       oy = Y + destY*zoom
sx = destW/500               sy = destH/srcH
```

`canvasToScreenPoint` / `screenToCanvasPoint` 是这套映射的正 / 逆变换，所有命中判定都要先把鼠标坐标转换到角色画布坐标系。

DOM 事件坐标转主画布坐标另有一套换算（`toGameCoords`），按 canvas 元素的 `clientWidth/Height` 做等比缩放：

```js
x = (e.clientX - canvas.offsetLeft) * 2000 / canvas.clientWidth
y = (e.clientY - canvas.offsetTop) * 1000 / canvas.clientHeight
```

### 4.2 alpha 边界框：先粗筛

一张贴图的画布通常是共用尺寸（比如 500×1000），但实际图案往往只占其中一小块，其余是透明留白。直接拿整幅贴图当命中范围会导致"点空白处也算命中"。

`getAlphaData` 会把贴图画到一个离屏 canvas 上，用 `getImageData` 读像素，扫描出 alpha > 阈值的像素范围，得到一个贴合内容的最小矩形（`bounds`），同时生成一份降采样后的位图掩码（`mask`，每轴缩小 4 倍）供后续逐像素判定使用。这个结果按 URL 缓存（LRU，上限 500 条），避免重复扫描。

```js
const alpha = getAlphaData(url, img);   // { bounds:{x,y,w,h}, mask, mw, mh, scale }
```

### 4.3 像素级二次判定：精确排除空白区域

光落在包围矩形内还不够——衣服的形状往往不规则（袖子之间、裙摆缺口都是透明的）。`pickAt` 在矩形命中后，再用降采样掩码做一次精确查询：

```js
function isOpaqueAt(alpha, px, py) {
    const mx = px / alpha.scale | 0, my = py / alpha.scale | 0;
    return alpha.mask[my * alpha.mw + mx] === 1;
}
```

只有真正落在不透明像素上，这件装备才算被命中。

### 4.4 多件重叠时的层叠排序 + 循环切换

一个位置可能同时命中多件装备（外套盖着衬衫）。`pickAt` 收集所有命中项后，按**层叠顺序**（`layerStackOrder`，即 `C.AppearanceLayers` 里的下标，数值越大越靠上）从上到下排序，这样第一次点击总是选中"看得见的最外层"那一件。

再次点击**同一位置**（容差为句柄半径的 2 倍）时，`cycleIndex` 会在候选列表中往后移一位，从而"轮换"选中下一层的衣服；若位置变了或候选集变了，则从头（最上层）重新开始：

```js
sameSpotIndex(hits, mx, my) {
    if (Math.hypot(mx - last.x, my - last.y) > SAME_SPOT) return -1;
    if (last.key !== hits.map(h => h.group.Name).join(",")) return -1;
    return hits.findIndex(h => h.group.Name === last.groupName);
}
```

---

## 五、悬停外框：如何画出"贴着轮廓"的描边

如果只是画命中矩形的边框，效果会很粗糙（矩形套住不规则轮廓，视觉上很难看清具体是哪一块）。这里用了一套**纯 Canvas2D 合成操作**实现的"轮廓外扩描边"，不逐像素读取、全程走 GPU：

原理分三步（`drawAppearanceHover`，与调色界面 `LayerTransformGizmo.drawOutline` 同一手法）：

1. **环向偏移叠加**：把该装备的贴图按若干个角度方向（16 个采样方向）各偏移 `OUTLINE.width` 像素，用 `globalCompositeOperation = "lighter"` 叠加画到一个离屏 canvas 上。由于 alpha 值是累加的，得到的结果相当于原图案"膨胀"了一圈的形状。

   ```js
   octx.globalCompositeOperation = "lighter";
   for (let i = 0; i < OUTLINE.samples; i++) {
       const a = i / OUTLINE.samples * Math.PI * 2;
       blit(Math.cos(a) * OUTLINE.width, Math.sin(a) * OUTLINE.width);
   }
   ```

2. **抠掉原图**：用 `destination-out` 把未偏移的原始图案减去，只留下"膨胀圈"那一圈像素。

   ```js
   octx.globalCompositeOperation = "destination-out";
   blit(0, 0);
   ```

3. **统一染色**：用 `source-in` 把剩下的形状整体填成高亮色。

   ```js
   octx.globalCompositeOperation = "source-in";
   octx.fillStyle = GIZMO_STYLE.hoverStroke;
   octx.fillRect(0, 0, bw, bh);
   ```

最后把这块离屏 canvas 贴回主画布对应区域，再在轮廓上方画一个半透明标签条，显示"部件名 - 服装名"。

这套方法的好处：不需要计算多边形轮廓路径，也不需要逐像素扫描求边界，纯靠 Canvas 合成模式的数学性质得到贴合像素形状的描边，性能开销主要是几次 `drawImage`，可以每帧重绘。

---

## 六、点击拾取：跳转到编辑界面

命中判定复用上面的 `pickAt`/`cycleIndex`，选中目标后调用 `openClothScreen(group)`，这一步是照搬 BC 本体 `AppearanceClick` 里点击部位名时的内部逻辑：

```js
function openClothScreen(group) {
    const C = CharacterAppearanceSelection;
    C.FocusGroup = group;                       // 设置当前编辑的部位
    DialogInventoryBuild(C, true, false);        // 构建该部位的库存列表
    AppearancePreviewBuild?.(C, true);            // 构建预览
    CharacterAppearanceCloth = InventoryGet?.(C, group.Name) ?? null; // 记录取消时的回滚值
    CharacterAppearanceMode = "Cloth";            // 切到"选衣服"模式
    AppearanceMenuBuild?.(C);                     // 重建菜单按钮（可用操作随部位而变）
}
```

这五步缺一不可：`FocusGroup` 决定库存列表的目标部位，`CharacterAppearanceCloth` 是取消编辑时用来回滚显示的旧值，`CharacterAppearanceMode` 切换才能让界面真正显示到"选衣服"界面。

---

## 七、事件挂载与开关控制

- **悬浮更新**：Hook `CommonMouseMove`（转换坐标后调用 `updateHover`）+ `AppearanceRun` 结束时按当前鼠标位置主动刷新一次（应对"点击跳转后画面变了但鼠标没动"的情况）。
- **点击拦截**：Hook `AppearanceClick`，若 `appearancePicker.click()` 返回 `true`（命中并成功跳转），直接 `return` 不再调用原函数，从而"吞掉"这次点击，避免触发底层其它按钮逻辑。
- **可用范围限制** (`isEnabled`)：仅在换装界面的默认模式或 Cloth（选衣服）模式下生效；扩展物品面板打开、或处于图层调整（Layering）界面时禁用，避免抢占它们的交互；同时把可点击的横纵坐标范围限制在全身图区域内（右侧留出给列表/按钮，顶部留出给菜单按钮行），避免和原生 UI 打架。
- **总开关**：受设置项 `ClothPickEnabled`（"点击服装拾取"）统一控制，关闭时立刻清空悬浮态和描边，不残留视觉痕迹。
- **失效重建**：Hook `CharacterLoadCanvas`，角色重绘（换衣服、颜色改变等）后调用 `invalidate()` 清空旧的 `captures`（贴图已经变了，旧坐标不再有效），但保留 `archive` 存档以配合闪烁提示（除非本来就没有闪烁计时器在跑）。

---

## 八、与调色界面图层拾取（`LayerTransformGizmo`）的关系

`ItemColorAdjustmentWindow.gizmo`（`LayerTransformGizmo` 类）实现的是**单件衣服内部各图层**的拾取与变换句柄，用的是完全相同的三板斧：

1. Hook `GLDrawImage`/`DrawImageCanvas`，按贴图文件名匹配到 `Asset.Layer` 里的具体图层（`matchDrawLayerIndex`）。
2. 用 alpha 边界框 + 降采样掩码做像素级命中判定（`isLayerOpaqueAt`）。
3 用同样的"环向偏移 + 抠图 + 染色"技术画轮廓描边（`drawOutline`）。

区别在于它还额外支持对选中图层做**拖拽平移 / 缩放 / 旋转**（`startDrag`/`applyMove`/`applyScale`/`applyRotate`），这部分建立在一套"贴图空间 ↔ 角色画布坐标"的仿射变换换算之上（支点固定在整幅贴图中心，与 BC 本体 `GLDrawImage` 的矩阵链保持一致），属于比拾取更进一步的编辑功能，不在本文讨论范围内。

---

## 九、小结：核心技术点

| 问题 | 解决方案 |
|---|---|
| 渲染管线不暴露"贴图属于哪个 Item" | Hook 绘制函数，按贴图 URL 文件名反查 Asset/图层 |
| 主画布坐标 ↔ 贴图坐标换算 | 记录 `DrawCharacter` 的 x/y/zoom，推导线性映射 |
| 矩形包围盒不精确 | 逐像素扫描生成 alpha 边界 + 降采样掩码，二次判定 |
| 多件贴图重叠如何选中 | 按角色图层层叠序排序 + 同位置连续点击循环切换 |
| 高亮框需要贴合不规则轮廓 | Canvas2D 合成操作：环向偏移叠加 → 抠除原图 → 统一染色 |
| 命中即可跳转到对应界面 | 复刻本体 `AppearanceClick` 内部切换 Cloth 模式的五步逻辑 |
